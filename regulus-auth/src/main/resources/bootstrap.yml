spring:
  application:
    name: regulus-auth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  profiles:
    active: ${profiles.active}